# Server Configuration
# server.port=8080
# server.address=0.0.0.0
#server.address = 192.168.1.27
#server.port=8080
server.port=${PORT:8080}



# PassKit Configuration
# Paths relative to project root when running from IntelliJ
# Try 'certs/' first, fallback to 'backend/certs/' for Docker
passkit.certificate.path=certs/pass-certificate.pem
passkit.privatekey.path=certs/pass-private-key.pem
passkit.wwdr.path=certs/wwdr.pem

# Pass Information
passkit.pass.typeIdentifier=pass.com.codecraft.PassKitPOC
passkit.pass.teamIdentifier=JEAXKNJ584
passkit.pass.organizationName=Codecraft Org

# Logging
logging.level.com.example.passkit=DEBUG
logging.level.org.springframework.web=INFO

# Database Configuration (H2 in-memory for development)
spring.datasource.url=jdbc:h2:mem:passkit
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Apple Wallet Web Service Configuration
passkit.webservice.url=https://your-domain.com
# For development, use: passkit.auth.token=dev-bypass
# For production, generate a strong random token
passkit.auth.token=vxwxd7J8AlNNFPS8k0a0FfUFtq0ewzFdc
